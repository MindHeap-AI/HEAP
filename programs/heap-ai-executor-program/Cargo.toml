[package]
name = "heap-ai-executor-program"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
solana-program = { workspace = true }
borsh = { workspace = true, features = ["derive"] }
